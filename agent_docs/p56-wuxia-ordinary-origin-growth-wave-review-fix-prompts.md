# P56 Ordinary Origin Growth Wave — Fix Prompts

> Generated by A1 Planner-Verify
> Status: BLOCKED (no implementation exists; prd.json passes: true is premature)

## Execution Order

### FIX-001 [required] — Gap Audit + Scope Contract (US-001, US-002)
> 在 `docs/PRD/p56-wuxia-ordinary-origin-growth-wave.prd.json` 中将 P56-001 和 P56-002 的 `passes` 设为 `false`（因为它们尚未被实施）。
>
> 然后执行 P56-001：创建 `docs/test-reports/p56-ordinary-origin-growth-gap-audit.md`，汇总三个 ordinary origins (farm_peasant, town_apprentice, tavern_hand) 的 proven surfaces 与薄弱点，区分已 Met 的最低准则与可增长的玩家体验层。不改运行行为。
>
> 执行 P56-002：创建 `docs/test-reports/p56-ordinary-origin-growth-scope-contract.md`，明确只处理三个 origin、允许层（剧情配置/轻量展示/验证脚本）、禁止项（第四 origin/sample-line/全量平民系统/批量 deferred 接线）。

### FIX-002 [required] — Design Contracts (US-003, US-004, US-005)
> 执行 P56-003：定义 `farm_peasant` 的 bounded growth contract，至少 2 个 peasant-specific 中期信号或分岔（至少 1 个与"坚守/换路/积累"相关），不把 peasant 改写成 vivid origin。规格写入 PRD 附录或 audit 文档。
>
> 执行 P56-004：定义 `town_apprentice` 的 bounded growth contract，至少 2 个 apprentice-specific 中期信号（至少 1 个与 trade/craft/magnate opportunity 相关），保持 ordinary identity。
>
> 执行 P56-005：定义 `tavern_hand` 的 bounded growth contract，至少 2 个 tavern-specific 中期信号（至少 1 个与 ally network/guest circulation/chance meeting 相关），保持 ordinary identity。
>
> 更新 prd.json 中 P56-003/004/005 为 `passes: true`（实施后）。

### FIX-003 [required] — Story Configuration Wiring (US-006)
> 执行 P56-006：为三条 ordinary paths 各新增至少 1 个中期 story 配置接点。不引入新 origin framework。确保早期 wiring 与既有 P25 regression 不退化。相关 targeted tests 可验证。
>
> 修改文件应在 `src/data/lines/` 下的 origin 配置文件中添加 midlife 分岔事件，或在相关 spine/事件文件中新增 ordinary-specific 配置接点。
>
> 更新 prd.json 中 P56-006 为 `passes: true`。

### FIX-004 [required] — Player-Facing Expression (US-007)
> 执行 P56-007：为三条 ordinary origins 各新增至少 1 个可读表达信号（currentGoal/life-memory/summary），说明其不同机会结构。不新增 UI 组件。
>
> 修改应在 expression 相关文件（如 `src/p50/sampleLineExpression.ts` 或类似的 ordinary expression 文件）中添加 ordinary-specific 表达分支。
>
> 更新 prd.json 中 P56-007 为 `passes: true`。

### FIX-005 [required] — Mid-Tier Verification Slice (US-008)
> 执行 P56-008：创建至少 1 份 mid-tier/midlife ordinary verification artifact，涵盖三条 ordinary origins 的差异点，可读展示关键 flags/events/summaries。不要求 full lifetime exhaust。
>
> 输出到 `docs/test-reports/p56-ordinary-origin-midlife-verification-slice.md`。
>
> 更新 prd.json 中 P56-008 为 `passes: true`。

### FIX-006 [required] — Regression Tests (US-009)
> 执行 P56-009：为三条 ordinary origins 各补至少 1 条窄断言（regression test），复用现有 P25/origin/simulation harness，不重写全量 P25 tests。
>
> 新建 `tests/p56OrdinaryOriginGrowthTests.ts`，包含至少 3 条 origin-specific 断言。
>
> 验证：运行相关测试命令确认 Pass。
>
> 更新 prd.json 中 P56-009 为 `passes: true`。

### FIX-007 [required] — Closure Report (US-010)
> 执行 P56-010：创建 `docs/test-reports/p56-ordinary-origin-growth-closure-report.md`，汇总配置、表达、验证、测试证据，确认 P25 Wave 4 minimum remains valid and unchanged，列出仍 deferred 的 ordinary-content 扩展项。
>
> 更新 prd.json 中 P56-010 为 `passes: true`。

### FIX-008 [required] — Fix Premature prd.json State
> 在完成 FIX-001 到 FIX-007 后，更新 `docs/PRD/p56-wuxia-ordinary-origin-growth-wave.prd.json` 中所有 story 的 `passes` 字段为实际状态。仅在代码/文档确实实施并验证通过后才设为 `true`。
>
> 在 `progress.txt` 中添加 P56 相关进度条目。
