# Copy to .env.development.local (gitignored via *.local)
# Default: API-backed product path for `npm run dev` (requires `npm run p6b:serve`).
VITE_P6B_API_URL=http://localhost:8787
# To use offline local engine only, remove or comment out VITE_P6B_API_URL and restart Vite.
